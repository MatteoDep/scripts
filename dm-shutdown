#!/bin/sh

options="lock
suspend
shutdown
reboot"

choice=$(echo "$options" | dmenu -p "select: ")

case "$choice" in
    "lock") slock;;
    "suspend") loginctl suspend;;
    "shutdown") loginctl shutdown;;
    "reboot") loginctl reboot;;
    *) exit
esac
